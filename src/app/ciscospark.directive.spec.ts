import { CiscosparkDirective } from './ciscospark.directive';

describe('CiscosparkDirective', () => {
  it('should create an instance', () => {
    const directive = new CiscosparkDirective();
    expect(directive).toBeTruthy();
  });
});
